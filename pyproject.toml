[project]
name = "thalamus"
version = "0.1.0"
requires-python = ">=3.10"

dependencies = [
  "nats-py",
  "httpx",
  "jsonschema",
  "pytest",
  "pytest-asyncio"
]

[tool.pytest.ini_options]
pythonpath = ["."]
filterwarnings = [
    "ignore::DeprecationWarning:testcontainers.*"
]